/// <reference types="react" />
import { BaseIconProps } from './types';
interface LockIconProps extends BaseIconProps {
    size?: '15';
    type?: 'normal' | 'twoTone';
}
export declare const LockIcon: ({ color, size, type, ...props }: LockIconProps) => JSX.Element;
export default LockIcon;
